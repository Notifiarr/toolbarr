linters:
  enable-all: true
  disable:
  # deprecated
    - maligned
    - scopelint
    - interfacer
    - golint
    - exhaustivestruct
    - nosnakecase
    - structcheck
    - deadcode
    - varcheck
    - ifshort
    # unused
    - exhaustruct
    - exhaustive
    - containedctx
    - nlreturn
    - contextcheck
    # # unneeded (broken because of generics)
    - rowserrcheck
    - wastedassign
run:
  timeout: 5m